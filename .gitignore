node_modules
dist
.env
.env.local
.DS_Store

# Astro's generated types and cache. Regenerated by astro dev, build and sync,
# so tracking it only produced churn: content.d.ts changes whenever the content
# schema does, and a dirty copy blocks rebases.
.astro/
